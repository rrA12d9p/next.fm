var client_id = '<%=ENV["sound_cloud_client_id"]%>';
var redirect_uri = 'http://localhost:3000/';

// initialize client with app credentials
SC.initialize({
  client_id: client_id,
  redirect_uri: redirect_uri
});

